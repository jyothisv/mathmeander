// @mathmeander/schema — the generated contract package.
// Everything under src/generated/ is emitted from the Rust core's schema artifact
// (see scripts/generate.ts) — never hand-edited. This index is the ONE hand-written
// file: re-exports only.
export * from './generated/schemas';
export * from './generated/types';
export * from './generated/artifact-hash';
